spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/milly}
spring.datasource.username=${DATABASE_USERNAME:milly}
spring.datasource.password=${DATABASE_PASSWORD:milly}

spring.jpa.hibernate.ddl-auto=validate

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
