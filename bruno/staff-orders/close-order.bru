meta {
  name: Close order
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/venues/{{testVenueId}}/orders/{{testOrderId}}/close
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
