meta {
  name: List orders by status
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/venues/{{testVenueId}}/orders?status=PENDING
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
